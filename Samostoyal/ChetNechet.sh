#!/bin/bash
# Скрипт определяет, чётное или нечётное число

echo "Введите целое число:"
read num

if ! [[ "$num" =~ ^-?[0-9]+$ ]]; then
    echo "Ошибка: нужно ввести целое число!"
    exit 1
fi

if (( num % 2 == 0 )); then
    echo "Число $num — чётное"
else
    echo "Число $num — нечётное"
fi